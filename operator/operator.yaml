defaultNamespace: rabbitmq-system

# Define the Helm charts to be deployed
# your-gitops-repo/rabbitmq-minimal/fleet.yaml

# Configuration for deploying the RabbitMQ Cluster Operator Helm chart

helm:
  # Using the Bitnami chart for the Operator
  chart: bitnami/rabbitmq-cluster-operator
  repo: https://charts.bitnami.com/bitnami
  # No special values are typically needed for the Operator deployment itself.

